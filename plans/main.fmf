# Use the internal executor
execute:
    how: shell
    script:
        - lsblk
        - lsblk -f
        - lsblk -l
        - free -h
        - uname -a
        - ls -la /
        - ls -la /boot/
        - ls /boot/grub2/device.map
        - cat /boot/grub2/device.map
        - ls /sys/firmware/
